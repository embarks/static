import { styled } from "@storybook/theming"
import Button from "../components/Button"
import Link from "../components/Link"
import pckg from "../../package.json"

<Meta title="README" />

On the side of your screen there is a list of links. You can click on it... ... ... or not. Contained are my javascript widgets and documents I have written.

export const CurrentVersion = () => {
  return <strong>{pckg.version}</strong>
}

version: <CurrentVersion />

export const UL = styled.ul`
  margin-left: 0;
  padding-left: 0;
  & > li {
    list-style: none;
  }
`

export const Container = styled.div`
  display: flex;
`

export const DependencyList = () => {
  const formatDependency = (dependency, version) => {
    return (
      <>
        <strong>{`${dependency} `}</strong>
        {`${version}`}
      </>
    )
  }
  const dependencies = Object.keys(pckg.dependencies).map((dependency) => (
    <li key={dependency}>
      {formatDependency(dependency, pckg.dependencies[dependency])}
    </li>
  ))
  return (
    <>
      <Container> 
        <UL>{dependencies}</UL>
      </Container>
    </>
  )
}

<DependencyList />